package main

import "fmt"

func main() {
	fmt.Println("Hello, Sumer")
}

/*

Go is organized into packages, which are similar
to libraries and modules.
A package consists of one or more .go source files in a single directory.

The Go Standard library has over 100 packages for common
tasks like input and output, as well as sorting and text manipulation

Println is one of the main commands

Function Declaration consists of the keyword func, the name of a function,
a parameter list(empty for main), a result list(also empty here), and a body of a function


*/
